<Page x:Class="EasyPrinter.Views.CopyView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Копирование"
      Background="{StaticResource BackgroundBrush}">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Button Style="{StaticResource BackButtonStyle}"
                    HorizontalAlignment="Left"
                    Click="BackButton_Click"/>
            <TextBlock Text="КОПИРОВАНИЕ"
                       Style="{StaticResource TitleStyle}"
                       HorizontalAlignment="Center"/>
        </Grid>

        <!-- Основной контент -->
        <Grid Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Border Style="{StaticResource CardStyle}" MinWidth="500">
                <StackPanel>
                    <!-- Иконка -->
                    <TextBlock Text="&#128203;"
                               FontSize="100"
                               HorizontalAlignment="Center"
                               Margin="0,0,0,30"/>

                    <TextBlock Text="Положите документ на стекло сканера или в автоподатчик"
                               Style="{StaticResource NormalTextStyle}"
                               HorizontalAlignment="Center"
                               TextAlignment="Center"
                               Foreground="{StaticResource TextSecondaryBrush}"
                               Margin="0,0,0,40"/>

                    <!-- Настройки копирования -->
                    <Grid Margin="0,0,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Количество копий -->
                        <StackPanel Grid.Column="0" Margin="0,0,20,0">
                            <TextBlock Text="Количество копий:"
                                       Style="{StaticResource LabelStyle}"/>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <Button Grid.Column="0"
                                        Content="-"
                                        Width="60" Height="60"
                                        FontSize="24"
                                        Click="DecreaseCopies_Click"/>

                                <TextBlock x:Name="CopiesText"
                                           Grid.Column="1"
                                           Text="1"
                                           FontSize="48"
                                           FontWeight="Bold"
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Center"/>

                                <Button Grid.Column="2"
                                        Content="+"
                                        Width="60" Height="60"
                                        FontSize="24"
                                        Click="IncreaseCopies_Click"/>
                            </Grid>
                        </StackPanel>

                        <!-- Масштаб -->
                        <StackPanel Grid.Column="1" Margin="20,0,0,0">
                            <TextBlock Text="Масштаб:"
                                       Style="{StaticResource LabelStyle}"/>
                            <ComboBox x:Name="ScaleComboBox"
                                      Style="{StaticResource LargeComboBoxStyle}"
                                      SelectedIndex="2">
                                <ComboBoxItem Content="50%"/>
                                <ComboBoxItem Content="75%"/>
                                <ComboBoxItem Content="100% (Оригинал)"/>
                                <ComboBoxItem Content="125%"/>
                                <ComboBoxItem Content="150%"/>
                                <ComboBoxItem Content="200%"/>
                            </ComboBox>
                        </StackPanel>
                    </Grid>

                    <!-- Качество -->
                    <TextBlock Text="Качество:" Style="{StaticResource LabelStyle}"/>
                    <ComboBox x:Name="QualityComboBox"
                              Style="{StaticResource LargeComboBoxStyle}"
                              SelectedIndex="1">
                        <ComboBoxItem Content="Черновик (быстро)"/>
                        <ComboBoxItem Content="Нормальное"/>
                        <ComboBoxItem Content="Высокое"/>
                    </ComboBox>

                    <!-- Источник -->
                    <TextBlock Text="Источник:" Style="{StaticResource LabelStyle}"/>
                    <ComboBox x:Name="SourceComboBox"
                              Style="{StaticResource LargeComboBoxStyle}"
                              SelectedIndex="0">
                        <ComboBoxItem Content="Стекло сканера"/>
                        <ComboBoxItem Content="Автоподатчик (АПД)"/>
                    </ComboBox>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Кнопка копирования -->
        <Button x:Name="CopyButton"
                Grid.Row="2"
                Content="КОПИРОВАТЬ"
                Style="{StaticResource ActionButtonStyle}"
                Background="#FF9800"
                Height="100"
                FontSize="32"
                Margin="0,20,0,0"
                Click="CopyButton_Click"/>
    </Grid>
</Page>
